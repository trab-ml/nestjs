import {
  Body,
  Controller,
  Delete,
  Get,
  Header,
  HttpCode,
  Param,
  Patch,
  Post,
  Query,
  Redirect,
} from '@nestjs/common';

@Controller('clients')
export class ClientsController {
  /*
    GET /clients
    GET /clients/:id
    POST /clients
    PATCH /clients/:id
    DELETE /clients/:id
    */
}
